<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item android:id="@+id/my_profile"
            android:icon="@drawable/ic_user_profile"
            android:title="Profile"/>
        <item android:id="@+id/notification"
            android:icon="@drawable/ic_notifications"
            android:title="Notification"/>
        <item android:id="@+id/my_favorite"
            android:icon="@drawable/ic_favorite_spot"
            android:title="Saved"/>
        <item android:id="@+id/feedback_concern"
            android:icon="@drawable/ic_feedback_concern"
            android:title="Feedback &amp; Concern"/>
    </group>

    <item android:title="About &amp; Exit session">
        <menu>
            <group android:checkableBehavior="single">
                <item android:id="@+id/about"
                    android:icon="@drawable/ic_info"
                    android:title="About"/>
                <item android:id="@+id/logout"
                    android:icon="@drawable/ic_logout"
                    android:title="Logout"/>
            </group>
        </menu>
    </item>
</menu>